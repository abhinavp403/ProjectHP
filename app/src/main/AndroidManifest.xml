<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhinav.projecthp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreenActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotPasswordActivity" />
        <activity android:name=".LandingPageActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".EnrollmentActivity" />
        <activity android:name=".NewStudentActivity" />
        <activity android:name=".StudentNamesActivity" />
        <activity android:name=".StudentDetailsActivity" />
        <activity android:name=".NewTrainerActivity" />
        <activity android:name=".TrainerNamesActivity" />
        <activity android:name=".TrainerDetailsActivity" />
        <activity android:name=".NewCourseActivity" />
        <activity android:name=".CourseNamesActivity" />
        <activity android:name=".CourseDetailsActivity" />
    </application>

</manifest>